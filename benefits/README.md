# 💳 ההטבות שלנו

מערכת לניהול ההטבות של מועדוני הלקוחות וכרטיסי האשראי שלנו, ולשאלה הפשוטה:
**מאיפה הכי כדאי לנצל?**

האתר רץ בדפדפן בלבד, בלי שרת ובלי בסיס נתונים, ולכן האחסון שלו חינמי לגמרי.

---

## מה המערכת עושה

| לשונית | למה היא טובה |
|---|---|
| **שאלה** | כותבים שאלה חופשית ("איפה כדאי לקנות מקרר?") ומקבלים את ההטבות הרלוונטיות, מהמשתלמת לפחות משתלמת |
| **השוואה** | בוחרים קטגוריה וסכום, והמערכת מחשבת כמה שקלים חוסכים בכל מועדון וכרטיס ומדרגת |
| **עיון** | דפדוף בכל ההטבות, עם סינון לפי מועדון ולפי הטבות שעומדות לפוג |
| **ניהול** | הוספה ועריכה של הטבות |
| **הגדרות** | שווי נקודה, סנכרון, קוד כניסה |

החישוב יודע להתמודד עם אחוזי הנחה, סכומים קבועים, החזר כספי, 1+1,
צבירת נקודות, **תקרות לעסקה** ו**מינימום קנייה**.

התקרה היא הסיבה המרכזית שכדאי להשתמש בהשוואה ולא באינטואיציה:
25% הנחה עם תקרה של 60 ₪ שווה פחות מ-10% בלי תקרה ברגע שהקנייה עוברת 600 ₪.

---

## הפעלה ראשונה

1. פותחים את כתובת האתר.
2. קוד הכניסה ההתחלתי הוא **1234**. מחליפים אותו מיד בלשונית "הגדרות".
3. עוברים על ההטבות בלשונית "עיון" ומאמתים אותן — ראו למטה.

---

## ⚠️ חשוב מאוד: נתוני הבסיס טעונים אימות

כל ההטבות שנטענו מראש מסומנות בתג כתום **"לאימות"**. הן נאספו ממקורות
פומביים באינטרנט, ולא מהחשבון האישי שלכם. חלק מהאתרים (כאל, הייטקזון)
כלל לא היו נגישים בזמן האיסוף, והנתונים לגביהם הגיעו מסיקורים עקיפים.

**אין להסתמך על מספר שמסומן "לאימות" לפני רכישה.**

התהליך הנכון:

1. נכנסים לאתר או לאפליקציה של המועדון ורואים את ההטבה במו עינינו.
2. בלשונית "ניהול" לוחצים "ערוך" על ההטבה, מתקנים את המספרים והתנאים.
3. משנים סטטוס ל־**"מאומת"** ומדביקים קישור למקור.

ככל שיותר הטבות יסומנו "מאומת", כך התשובות של המערכת שוות יותר.
החותמת בראש המסך מראה כמה עוד נשארו לאימות.

---

## איך שנינו רואים את אותם נתונים

השינויים נשמרים בדפדפן שבו ערכתם אותם. כדי לשתף יש שתי דרכים:

### הדרך הנוחה — סנכרון אוטומטי

1. ב-GitHub: `Settings` ← `Developer settings` ← `Personal access tokens`
   ← `Fine-grained tokens` ← `Generate new token`.
2. תחת `Repository access` בוחרים **רק** את הריפו הזה.
3. תחת `Permissions` נותנים `Contents: Read and write`. שום הרשאה נוספת.
4. מעתיקים את הטוקן, ומדביקים אותו בלשונית "הגדרות" ← "סנכרון עם GitHub".
5. מעכשיו, אחרי כל שינוי לוחצים **"סנכרון ל-GitHub"** בלשונית "ניהול".
6. בצד השני לוחצים **"משוך עדכונים מהשרת"** כדי לקבל את השינויים.

הטוקן נשמר בדפדפן שלכם בלבד ונשלח רק ל-GitHub.

### הדרך הפשוטה — ייצוא וייבוא

בלשונית "ניהול": "ייצוא לקובץ", שולחים את הקובץ, והצד השני עושה "ייבוא מקובץ".

---

## 🔒 על הפרטיות — קראו את זה

**הריפו הזה ציבורי, ולכן כל מה שנשמר כאן גלוי לכל אחד באינטרנט.**

קוד הכניסה מונע כניסה מקרית בלבד. הוא **לא** הגנה אמיתית — כל הקוד של
האתר יורד לדפדפן, וכל מי שיודע להסתכל בו יראה את הקוד.

לכן:

- ❌ **אין** להזין מספרי כרטיס אשראי, מספרי חבר, תעודות זהות, סיסמאות
  או כל פרט מזהה.
- ✅ **מותר** להזין תיאורי הטבות — זה ממילא מידע שיווקי פומבי.

### רוצים פרטיות אמיתית?

יש דרך חינמית: להעביר את הריפו למצב פרטי ולארח ב-**Cloudflare Pages**
במקום ב-GitHub Pages. השירות חינמי, תומך בריפו פרטי, ומאפשר להוסיף
סיסמה אמיתית ברמת השרת (Cloudflare Access, חינם עד 50 משתמשים).
GitHub Pages לא מאפשר לארח ריפו פרטי בחשבון חינמי.

---

## העלאה לאוויר (GitHub Pages)

1. ב-GitHub: `Settings` ← `Pages`.
2. תחת `Source` בוחרים `Deploy from a branch`.
3. בוחרים ענף `master` ותיקייה `/ (root)`, ושומרים.
4. אחרי דקה-שתיים האתר יהיה זמין בכתובת:
   `https://arik2.github.io/2015_04_Web/benefits/`
5. פותחים בנייד, ובתפריט הדפדפן בוחרים "הוסף למסך הבית" כדי שייראה כמו אפליקציה.

> הקוד נמצא כרגע בענף פיתוח. כדי שהאתר יעלה מ-`master`, צריך למזג את
> הענף `claude/credit-card-benefits-system-tch69c` ל-`master` תחילה.

---

## מבנה הקבצים

```
benefits/
├── index.html            המסכים
├── styles.css            עיצוב (כולל מצב כהה אוטומטי)
├── engine.js             לוגיקת החיפוש והחישוב — בלי תלות בממשק
├── app.js                חיבור הממשק לנתונים
├── data/benefits.json    מסד הנתונים
└── test/engine.test.js   בדיקות ללוגיקה
```

להרצת הבדיקות (דורש Node):

```bash
node benefits/test/engine.test.js
```

---

## מבנה רשומת הטבה

```json
{
  "id": "מזהה ייחודי",
  "provider": "מזהה המועדון או הכרטיס",
  "title": "כותרת ההטבה",
  "categories": ["supermarket"],
  "kind": "percent | cashback | fixed | bogo | points | info",
  "value": 15,
  "capPerTx": 100,
  "minSpend": 0,
  "requiresCard": "שם הכרטיס שחייבים לשלם בו, או null",
  "validUntil": "2026-12-31",
  "conditions": "תנאים בשפה חופשית",
  "source": "קישור למקור",
  "checkedAt": "2026-08-10",
  "status": "verified | unverified",
  "tags": ["מילות חיפוש נוספות"]
}
```

`kind` קובע איך מחושב החיסכון:

| ערך | משמעות של `value` |
|---|---|
| `percent` / `cashback` | אחוז |
| `fixed` | סכום בשקלים |
| `bogo` | לא בשימוש — החיסכון הוא עד מחצית הסכום |
| `points` | כמה שקלים נדרשים לנקודה אחת |
| `info` | לא בשימוש — הטבה שלא ניתן לכמת |

---

## מה המערכת לא עושה

- **לא מתחברת לחשבונות שלכם.** אין כאן שליפה אוטומטית מהבנק או מחברות
  האשראי. הן דורשות אימות דו-שלבי בסמס וחוסמות התחברות אוטומטית,
  ושמירת סיסמאות בנק באתר היא סיכון אמיתי. העדכון ידני, ובכוונה.
- **לא ממציאה הטבות.** התשובות מגיעות אך ורק ממסד הנתונים. אם משהו לא
  רשום — הוא לא יופיע, וזו התנהגות רצויה: עדיף "לא יודע" על פני מספר
  שנשמע נכון ועולה כסף.
